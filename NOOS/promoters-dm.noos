(define-domain-model promoters-DM
  (creator "Santiago Ontanon")
  (description "Domain model for the promoters data set")
  (uses promoters-ontology))
(define (ATT01-sort :id vt))
(define (ATT01-sort :id vg))
(define (ATT01-sort :id va))
(define (ATT01-sort :id vc))
(define (ATT02-sort :id va))
(define (ATT02-sort :id vg))
(define (ATT02-sort :id vt))
(define (ATT02-sort :id vc))
(define (ATT03-sort :id vc))
(define (ATT03-sort :id va))
(define (ATT03-sort :id vt))
(define (ATT03-sort :id vg))
(define (ATT04-sort :id vt))
(define (ATT04-sort :id vc))
(define (ATT04-sort :id va))
(define (ATT04-sort :id vg))
(define (ATT05-sort :id va))
(define (ATT05-sort :id vt))
(define (ATT05-sort :id vg))
(define (ATT05-sort :id vc))
(define (ATT06-sort :id vg))
(define (ATT06-sort :id vt))
(define (ATT06-sort :id va))
(define (ATT06-sort :id vc))
(define (ATT07-sort :id vc))
(define (ATT07-sort :id vg))
(define (ATT07-sort :id va))
(define (ATT07-sort :id vt))
(define (ATT08-sort :id va))
(define (ATT08-sort :id vc))
(define (ATT08-sort :id vt))
(define (ATT08-sort :id vg))
(define (ATT09-sort :id va))
(define (ATT09-sort :id vt))
(define (ATT09-sort :id vg))
(define (ATT09-sort :id vc))
(define (ATT10-sort :id vt))
(define (ATT10-sort :id vg))
(define (ATT10-sort :id va))
(define (ATT10-sort :id vc))
(define (ATT11-sort :id va))
(define (ATT11-sort :id vt))
(define (ATT11-sort :id vc))
(define (ATT11-sort :id vg))
(define (ATT12-sort :id vc))
(define (ATT12-sort :id vg))
(define (ATT12-sort :id va))
(define (ATT12-sort :id vt))
(define (ATT13-sort :id vg))
(define (ATT13-sort :id va))
(define (ATT13-sort :id vt))
(define (ATT13-sort :id vc))
(define (ATT14-sort :id vc))
(define (ATT14-sort :id vg))
(define (ATT14-sort :id va))
(define (ATT14-sort :id vt))
(define (ATT15-sort :id vt))
(define (ATT15-sort :id vg))
(define (ATT15-sort :id vc))
(define (ATT15-sort :id va))
(define (ATT16-sort :id vt))
(define (ATT16-sort :id vc))
(define (ATT16-sort :id vg))
(define (ATT16-sort :id va))
(define (ATT17-sort :id vg))
(define (ATT17-sort :id vt))
(define (ATT17-sort :id vc))
(define (ATT17-sort :id va))
(define (ATT18-sort :id vc))
(define (ATT18-sort :id vt))
(define (ATT18-sort :id va))
(define (ATT18-sort :id vg))
(define (ATT19-sort :id vg))
(define (ATT19-sort :id vc))
(define (ATT19-sort :id va))
(define (ATT19-sort :id vt))
(define (ATT20-sort :id vt))
(define (ATT20-sort :id va))
(define (ATT20-sort :id vg))
(define (ATT20-sort :id vc))
(define (ATT21-sort :id vt))
(define (ATT21-sort :id vc))
(define (ATT21-sort :id va))
(define (ATT21-sort :id vg))
(define (ATT22-sort :id vc))
(define (ATT22-sort :id vg))
(define (ATT22-sort :id va))
(define (ATT22-sort :id vt))
(define (ATT23-sort :id vg))
(define (ATT23-sort :id vc))
(define (ATT23-sort :id vt))
(define (ATT23-sort :id va))
(define (ATT24-sort :id vg))
(define (ATT24-sort :id vt))
(define (ATT24-sort :id vc))
(define (ATT24-sort :id va))
(define (ATT25-sort :id vt))
(define (ATT25-sort :id vg))
(define (ATT25-sort :id vc))
(define (ATT25-sort :id va))
(define (ATT26-sort :id vg))
(define (ATT26-sort :id va))
(define (ATT26-sort :id vt))
(define (ATT26-sort :id vc))
(define (ATT27-sort :id vg))
(define (ATT27-sort :id vt))
(define (ATT27-sort :id va))
(define (ATT27-sort :id vc))
(define (ATT28-sort :id vt))
(define (ATT28-sort :id vc))
(define (ATT28-sort :id vg))
(define (ATT28-sort :id va))
(define (ATT29-sort :id vt))
(define (ATT29-sort :id vg))
(define (ATT29-sort :id va))
(define (ATT29-sort :id vc))
(define (ATT30-sort :id va))
(define (ATT30-sort :id vg))
(define (ATT30-sort :id vt))
(define (ATT30-sort :id vc))
(define (ATT31-sort :id va))
(define (ATT31-sort :id vt))
(define (ATT31-sort :id vg))
(define (ATT31-sort :id vc))
(define (ATT32-sort :id vg))
(define (ATT32-sort :id vt))
(define (ATT32-sort :id va))
(define (ATT32-sort :id vc))
(define (ATT33-sort :id vt))
(define (ATT33-sort :id vc))
(define (ATT33-sort :id vg))
(define (ATT33-sort :id va))
(define (ATT34-sort :id va))
(define (ATT34-sort :id vc))
(define (ATT34-sort :id vt))
(define (ATT34-sort :id vg))
(define (ATT35-sort :id vt))
(define (ATT35-sort :id vg))
(define (ATT35-sort :id va))
(define (ATT35-sort :id vc))
(define (ATT36-sort :id vg))
(define (ATT36-sort :id vt))
(define (ATT36-sort :id va))
(define (ATT36-sort :id vc))
(define (ATT37-sort :id vt))
(define (ATT37-sort :id vg))
(define (ATT37-sort :id vc))
(define (ATT37-sort :id va))
(define (ATT38-sort :id va))
(define (ATT38-sort :id vg))
(define (ATT38-sort :id vc))
(define (ATT38-sort :id vt))
(define (ATT39-sort :id vt))
(define (ATT39-sort :id vc))
(define (ATT39-sort :id vg))
(define (ATT39-sort :id va))
(define (ATT40-sort :id va))
(define (ATT40-sort :id vc))
(define (ATT40-sort :id vt))
(define (ATT40-sort :id vg))
(define (ATT41-sort :id va))
(define (ATT41-sort :id vc))
(define (ATT41-sort :id vg))
(define (ATT41-sort :id vt))
(define (ATT42-sort :id vt))
(define (ATT42-sort :id vg))
(define (ATT42-sort :id vc))
(define (ATT42-sort :id va))
(define (ATT43-sort :id vg))
(define (ATT43-sort :id vc))
(define (ATT43-sort :id va))
(define (ATT43-sort :id vt))
(define (ATT44-sort :id vc))
(define (ATT44-sort :id vt))
(define (ATT44-sort :id va))
(define (ATT44-sort :id vg))
(define (ATT45-sort :id vg))
(define (ATT45-sort :id va))
(define (ATT45-sort :id vt))
(define (ATT45-sort :id vc))
(define (ATT46-sort :id vc))
(define (ATT46-sort :id va))
(define (ATT46-sort :id vg))
(define (ATT46-sort :id vt))
(define (ATT47-sort :id vg))
(define (ATT47-sort :id vc))
(define (ATT47-sort :id va))
(define (ATT47-sort :id vt))
(define (ATT48-sort :id vg))
(define (ATT48-sort :id vc))
(define (ATT48-sort :id vt))
(define (ATT48-sort :id va))
(define (ATT49-sort :id vg))
(define (ATT49-sort :id vc))
(define (ATT49-sort :id va))
(define (ATT49-sort :id vt))
(define (ATT50-sort :id vc))
(define (ATT50-sort :id va))
(define (ATT50-sort :id vg))
(define (ATT50-sort :id vt))
(define (ATT51-sort :id vt))
(define (ATT51-sort :id vc))
(define (ATT51-sort :id vg))
(define (ATT51-sort :id va))
(define (ATT52-sort :id vt))
(define (ATT52-sort :id vc))
(define (ATT52-sort :id va))
(define (ATT52-sort :id vg))
(define (ATT53-sort :id vg))
(define (ATT53-sort :id vc))
(define (ATT53-sort :id va))
(define (ATT53-sort :id vt))
(define (ATT54-sort :id vt))
(define (ATT54-sort :id vc))
(define (ATT54-sort :id vg))
(define (ATT54-sort :id va))
(define (ATT55-sort :id vc))
(define (ATT55-sort :id vg))
(define (ATT55-sort :id vt))
(define (ATT55-sort :id va))
(define (ATT56-sort :id vg))
(define (ATT56-sort :id va))
(define (ATT56-sort :id vc))
(define (ATT56-sort :id vt))
(define (ATT57-sort :id vt))
(define (ATT57-sort :id va))
(define (ATT57-sort :id vg))
(define (ATT57-sort :id vc))
(define (CLASS-sort :id v+))
(define (CLASS-sort :id v-))